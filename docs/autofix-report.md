# Autofix proposals: /tmp/autofix-sentences.txt

*Generated by `agenttest fix` — PROPOSALS ONLY, apply by hand after ADR 0012 meaning review.*


**UNRESOLVED** — minglish needs a lexicon: every allowed surface form, each with exactly one form-tag, plus redirects for rejected uses
  - flag: WORD: "minglish" is not a minglish word

**UNRESOLVED** — Three ways to produce it were considered: Derive it algorithmically from corpora/frequency lists and WordNet
  - flag: WORD: "Three" is not a minglish word — a name cannot start a sentence — introduce it ("the tool Lexgen …") or quote it; or if this is a command, use a minglish verb in lowercase ("delete the file")

**UNRESOLVED** — Hand-write "lexicon.tsv" directly and validate it
  - flag: WORD: "Hand-write" is not a minglish word — a name cannot start a sentence — introduce it ("the tool Lexgen …") or quote it; or if this is a command, use a minglish verb in lowercase ("delete the file")

**UNRESOLVED** — Hand-curate a small seed list (lemma + category + irregulars + redirects); generate the lexicon from it mechanically
  - flag: WORD: "Hand-curate" is not a minglish word — a name cannot start a sentence — introduce it ("the tool Lexgen …") or quote it; or if this is a command, use a minglish verb in lowercase ("delete the file")

**UNRESOLVED** — Prior research (docs/research/cnl-design-findings.md) showed that algorithmic word selection optimizes rarity, not clarity: a swap generator's apparent disambiguation was fully explained by picking rarer words, and 43% of its swaps had no benefit at all
  - flag: WORD: "Prior" is not a minglish word — a name cannot start a sentence — introduce it ("the tool Lexgen …") or quote it; or if this is a command, use a minglish verb in lowercase ("delete the file")

**UNRESOLVED** — Word choice must stay a human judgment
  - flag: WORD: "choice" is not a minglish word

**UNRESOLVED** — At the same time, hand-writing every inflected surface form (full paradigms, ~5 forms per verb) is toil that invites typos and silent gaps, and the critical safety property — no surface form with two tags, including cross-lemma inflection collisions like leaves — is exactly the kind of check humans miss and machines don't
  - flag: WORD: "same" is not a minglish word — for the thing already named write "the file"; for two things that match write "identical" ("the copies are identical") (ADR 0023)

**UNRESOLVED** — A hand-edited "seed/seed.json" is the single source of truth: one entry per lemma with its category, irregular-form overrides, "reject" redirects, optional "waive", and a free-text "note" recording the rationale
  - flag: WORD: "hand-edited" is not a minglish word

**UNRESOLVED** — A Rust tool ("crates/lexgen") expands paradigms with ~10 regular-morphology rules, enforces the linter invariants (collision-free, cross-POS completeness, no unattested forms) as hard build errors, and deterministically emits "lexicon.tsv" and "docs/lexicon-report.md"
  - flag: WORD: "(" is not a minglish word

**UNRESOLVED** — The maintainers commit the generated files
  - flag: not recognizable as a minglish-like sentence
- ✓ already minglish: People do not edit the generated files

**UNRESOLVED** — The maintainers vendor the reference data (WordNet 3.0 indices, mobypos, a zipf frequency table) and pin the data by checksum
  - flag: WORD: "vendor" is not a minglish word
- ✓ already minglish: The linter checks the words with the data
- ✓ already minglish: The data does not choose the words

**UNRESOLVED** — People choose the words; each word costs one JSON entry
  - flag: WORD: "words;" is not a minglish word

**UNRESOLVED** — The machine handles inflection and the safety checks
  - flag: WORD: "handles" is not a minglish word

**UNRESOLVED** — Every curation commit shows its effect in the diffs of "lexicon.tsv" and the metrics report, so iteration history lives in git log
  - flag: WORD: "curation" is not a minglish word

**UNRESOLVED** — The committed-generated-file pattern requires discipline (and eventually a CI check) that "lexicon.tsv" matches "seed.json"; a hand-edit to the TSV is a build error waiting to happen
  - flag: WORD: "committed-generated-file" is not a minglish word

**UNRESOLVED** — Reversing this later (e.g. moving to a database or to per-word files) means migrating accumulated curation, which gets more expensive as the seed grows — hence this ADR
  - flag: WORD: "Reversing" is not a minglish word — a name cannot start a sentence — introduce it ("the tool Lexgen …") or quote it; or if this is a command, use a minglish verb in lowercase ("delete the file")

**Summary:** 19 sentences — 3 already minglish, 0 proposals, 0 declared gaps, 16 unresolved.
