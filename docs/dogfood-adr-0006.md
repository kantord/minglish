# Dogfood: ADR 0006 in minglish

Rewritten in place 2026-09-02 with no model run (`just lint-file`, then fix a
gap or follow the advice per sentence); every unit parses.

Notes: the governing principle; five load terms (Cognitive Load, Open Dependency, Dependency Length, Embedding Depth, Context Need) and Language Model; the ranked criteria became an Enumeration plus explicit `X outranks Y` sentences, since ordered lists are deferred; the comparative claim 'length is cheaper than load' is declared as a `comparative` drop ('the maintainers accept a long text for a small load').

Meaning review per ADR 0012: the rewrite keeps every claim of the source;
register and ranking drops as noted. Pairs for these four ADRs were not
added to corpus/dogfood-pairs.tsv — the ADR text is the record.
