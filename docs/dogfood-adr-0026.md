# Dogfood: ADR 0026 in minglish

Rewritten in place 2026-09-02 with no model run (`just lint-file`, then fix a
gap or follow the advice per unit); every unit parses. The problem
archetypes met are logged in docs/rewrite-archetypes.md; the meaning review
(ADR 0012) happens in the batch review after all ADRs parse.
