# Dogfood: ADR 0007 in minglish

Rewritten in place 2026-09-02 with no model run (`just lint-file`, then fix a
gap or follow the advice per sentence); every unit parses.

Notes: the Conditional term; the not-enabled list became an Enumeration of the four words plus two sentences; 'garden paths' rendered as 'every wrong Parse at the seam'.

Meaning review per ADR 0012: the rewrite keeps every claim of the source;
register and ranking drops as noted. Pairs for these four ADRs were not
added to corpus/dogfood-pairs.tsv — the ADR text is the record.
