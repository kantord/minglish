[package]
name = "grammar"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
lalrpop-util = "0.22"

[build-dependencies]
lalrpop = { version = "0.22", default-features = false }

[dev-dependencies]
insta = "1"
