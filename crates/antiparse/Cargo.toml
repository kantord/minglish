[package]
name = "antiparse"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[dependencies]
grammar = { path = "../grammar" }
lalrpop-util = "0.22"

[build-dependencies]
lalrpop = { version = "0.22", default-features = false }
