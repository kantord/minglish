source: (language design)
sentence: only as a focus particle (e.g. 'used only for checking')
diagnosis: genuine precision device with placement ambiguity in English
resolution: gap
gap: design 'only' via its own grilling interview; first-token/fiat placement rules
  needed
